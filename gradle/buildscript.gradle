// Build-time repositories only.
repositories {
    mavenCentral()
    maven {
        url "https://plugins.gradle.org/m2/"
    }
}

// Build-time dependencies only.
dependencies {
    classpath "com.github.node-gradle:gradle-node-plugin:3.2.1"  // for NpmTask
}
